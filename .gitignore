# macOS / Windows
.DS_Store
Thumbs.db
desktop.ini

# SolidWorks lock + backup files
*.~*
~$*
*.bak
*.SLDBAK

# Editors
.vscode/
.idea/

# Photo working files (keep curated exports, not raw working files)
*.psd
*.lrcat
*.lrcat-journal

# A folder for raw camera files you don't want committed
/raw/

# Acoustics / FEA solver scratch files
*.cdb
*.rst
*.lock
abaqus_acis.log
# Stash-recovery sidecars
stash-recovered-*/
*.cr2
*.CR2
*.mov
*.MOV
*.mp4
*.MP4

# --- Proprietary Excel Plastics IP — NEVER commit (see NOTICE.md) ---
# Excel Plastics and its clients own the underlying CAD, drawings, and
# fabrication specs from the 2015–2017 employment. This repo documents the
# role + public-deployed work only. Defensive ignores so proprietary source
# never lands here by accident.
EP[0-9]*
EP-*
*excel-plastics*
*Excel-Plastics*
*ExcelPlastics*
/proprietary/
/client-cad/
# Proprietary CAD / drawing source (this is a docs+photos repo; no CAD belongs here)
*.sldprt
*.SLDPRT
*.sldasm
*.SLDASM
*.slddrw
*.SLDDRW
*.step
*.stp
*.iges
*.igs
*.x_t
*.dwg
